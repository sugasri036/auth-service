apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: investra

spec:

  type: ClusterIP

  selector:
    app: auth-service

  ports:

    - name: http
      port: 10000
      targetPort: 10000
      protocol: TCP